#include <stdio.h>

main()
{
    char pd[6];

    puts("Enter the password : ");
    gets(pd);

    if(strlen(pd) >= 6 && pd == )
    {
        printf("Your password is strong");
    }
    else{
        printf("Your password is not strong");
    }
}